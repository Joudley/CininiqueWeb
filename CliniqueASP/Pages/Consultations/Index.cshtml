@page
@model Clinique.Pages.Consultations.IndexModel

@{
    ViewData["Title"] = "Index";
}
<body style="background-color:lightgrey;">
    
</body>

 <div class="col-md-2">
            <a asp-page="Create" class="btn btn-primary mt-3"> Nouvelle consultation</a>
        </div>
</br>

@* <div>
<form>
    <p> 
       
        <input type="search" asp-for="SearchString" placeholder="Rechercher par idPatient"  class="form-control">
        <button type="submit" class="btn btn-primary m-2">Rechercher</button>

    
    </p>
</form>
</div> *@

        <hr>
        <hr>
        <h2 class="justify-content-center d-flex center mb-4">Liste des consultations</h2>

<table class="table table-striped table-gray">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Consultation[0].Medecin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Consultation[0].CodePatient)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Consultation[0].Poids)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Consultation[0].Hauteur)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Consultation[0].Diagnostique)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Consultation[0].ConsultationDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Consultation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Medecin.MedecinId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodePatient)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Poids)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hauteur)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Diagnostique)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConsultationDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ConsultationId" class="btn btn-dark">Editer</a> |
                <a asp-page="./Details" asp-route-id="@item.ConsultationId" class="btn btn-info style">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ConsultationId" class="btn btn-danger ">Effacer</a>
            </td>
        </tr>
}
    </tbody>
</table>
